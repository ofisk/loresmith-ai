# Optional - Cloudflare AI Gateway https://developers.cloudflare.com/ai-gateway/
# GATEWAY_BASE_URL=https://gateway.ai.cloudflare.com/v1/..

# Local development environment variables
# This file should be added to .gitignore to keep secrets secure
ADMIN_SECRET=your_admin_secret_here
OPENAI_API_KEY=sk-your-openai-api-key-here

# CORS allowed origins (comma-separated list)
# Development origins for local servers
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# API URL for local development
VITE_API_URL=http://localhost:8787

# For production, override this with your actual domain(s):
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# =============================================================================
# AutoRAG Configuration
# =============================================================================
# 
# SECURITY WARNING: Do NOT put API tokens in .vars files for production!
# Instead, set them in Cloudflare Dashboard Environment Variables:
# 1. Go to Workers & Pages → your-worker → Settings → Environment Variables
# 2. Add AUTORAG_API_TOKEN there (most secure)
# 3. Or use wrangler secret: wrangler secret put AUTORAG_API_TOKEN
#
# For local development only, you can use .dev.vars (ensure it's in .gitignore)

# AutoRAG API Configuration
# Base URL for Cloudflare AutoRAG API (don't change this)
AUTORAG_API_URL=https://api.cloudflare.com/client/v4

# Your Cloudflare Account ID (found in dashboard URL: dash.cloudflare.com/<account-id>)
AUTORAG_ACCOUNT_ID=your-cloudflare-account-id-here

# AutoRAG Search URL (your specific AutoRAG service endpoint)
AUTORAG_SEARCH_URL=https://api.cloudflare.com/client/v4/accounts/your-account-id/autorag/rags/your-rag-name/ai-search

# ⚠️  SECURITY: AutoRAG API Token
# DO NOT commit this to Git! Use Cloudflare Dashboard Environment Variables instead.
# For local dev only (ensure .dev.vars is in .gitignore):
# AUTORAG_API_TOKEN=your-autorag-api-token-here